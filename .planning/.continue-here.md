---
phase: company-data-isolation
task: complete
total_tasks: 4
status: complete
last_updated: 2026-05-07T11:52:17.704Z
branch: feature/accounting-customer-statements
feature_branch: feature/time-clock-service-hub (worktree at /home/magiccat/autoinvoice-features)
---

<current_state>
Company data isolation is COMPLETE across both worktrees. Every prisma.customer.create()
and prisma.invoice.create() now sets userId. Every list/get/delete query is scoped to
ctx.userId. TypeScript compiles clean (0 errors) on both worktrees. Backend is running
from /home/magiccat/autoinvoice-features on port 4000.
</current_state>

<completed_work>

## This session (2026-05-07)

- invoice.ts (both worktrees):
  - updateStatus: was using findUnique with no userId → now findFirst with userId check
  - delete: no ownership check → now findFirst verifies ownership before delete
  - downloadPdf: no userId on invoice fetch or user branding fetch → both scoped
  - stats: all 5 count/aggregate queries were global → now all filtered by ctx.userId

- smart-templates.ts service (both worktrees):
  - findCustomer(): added userId param, filters vector SQL and ORM queries by userId
  - quickAddCustomer(): added userId to signature + data, throws if missing
  - createQuickInvoice(): added userId guard + sets userId on invoice create

- smartTemplates.ts router (both worktrees):
  - quickAddCustomer mutation: now passes ctx.user.id to service

- tally.ts (both worktrees):
  - finalize: sets userId on the invoice it creates from the tally

- lead.ts, leadBusinessCard.ts, quote.ts, receipt.ts, voice.ts (main worktree + feature worktree):
  - All prisma.customer.create() and prisma.invoice.create() calls now set userId

- telegram/commands.ts (both worktrees):
  - Added getOwnerUserId() helper (looks up first active OWNER-role user)
  - createInvoiceFromData now scopes customer find+create and invoice create to owner

- Main worktree Prisma schema:
  - Added userId String + User relation + @@index([userId]) to Customer model
  - Added userId String + User relation + @@index([userId]) to Invoice model
  - Added customers Customer[] and invoices Invoice[] to User model
  - Ran prisma generate (no migration needed, DB already had the columns)

## Previous sessions
- invoice edit customer search autocomplete
- PDF generator fixes (service address, company, address line 2, payment terms, formatRate)
- DB migration adding userId to Customer (31 rows) and Invoice (63 rows), backfilled to William Donovan
- customer.ts router: full userId scoping
- invoice.ts router: list, get, create, update scoped
</completed_work>

<remaining_work>

Nothing remaining from the data isolation task. The branch feature/accounting-customer-statements
has clean uncommitted changes ready to commit.

Potential next work (not started):
- Time Clock 2.0 — spec at .planning/spec-time-clock-v2-calendar-shifts.md
- Customer statements feature (this branch name suggests it)
- Anything in the feature/time-clock-service-hub worktree
</remaining_work>

<decisions_made>

- Telegram bot uses getOwnerUserId() (first active OWNER-role user, ordered by createdAt)
  because Telegram has no tRPC auth context and the app is single-owner per deployment.

- findCustomer() in smart-templates.ts does two separate raw SQL paths (with/without userId)
  rather than dynamic SQL concatenation — keeps Prisma's tagged template parameterization for
  SQL injection safety.

- Both worktrees were kept in sync (feature worktree is the running backend, main worktree is
  the current git branch). All fixes applied to both.

- Schema changes to main worktree did NOT require a new migration because the DB already had
  the columns from the feature-branch migration. Only `prisma generate` was needed.
</decisions_made>

<blockers>
None. All TypeScript errors resolved, backend healthy.
</blockers>

<context>
The goal was multi-tenant data isolation: every user's data must be invisible to other users.
The pattern throughout is: add `userId: ctx.userId` (or `ctx.user.id`) to every CREATE, and
add `userId: ctx.userId` to every WHERE clause. The Telegram bot is the one exception because
it has no auth context — it falls back to the OWNER user.

The running server is autoinvoice-features worktree (port 4000/3000). The main worktree
(feature/accounting-customer-statements) has all changes staged but not committed.
</context>

<next_action>
Start with: commit the uncommitted changes on feature/accounting-customer-statements with
message "feat: complete company data isolation — scope all customer/invoice operations to userId"

Then decide: continue on this branch (customer statements feature?) or switch to
feature/time-clock-service-hub for Time Clock 2.0 work.

How it's served:
- Frontend: next-server from /home/magiccat/autoinvoice-features/apps/web on port 3000
- Backend: tsx src/index.ts from /home/magiccat/autoinvoice-features/apps/backend on port 4000
- Restart backend: pkill -f "autoinvoice-features.*tsx src/index"; cd /home/magiccat/autoinvoice-features/apps/backend && NODE_ENV=production /home/magiccat/autoinvoice-features/node_modules/.bin/tsx src/index.ts >> /tmp/autoinvoice-backend.log 2>&1 &
- DB backup at: /home/magiccat/AutoInvoice/backups/invoice_platform_20260504_072655.pgdump
- Always pg_dump before any DB changes
</next_action>
